{
	"name": "p5-tool-uploader",
	"version": "0.0.0",
	"private": true,
	"scripts": {
		"deploy": "wrangler deploy --env production",
		"dev": "wrangler dev --env development",
		"start": "wrangler dev --env development",
		"test": "vitest",
		"cf-typegen": "wrangler types"
	},
	"devDependencies": {
		"@cloudflare/vitest-pool-workers": "0.8.71",
		"typescript": "5.9.3",
		"vitest": "3.2.4",
		"wrangler": "4.58.0"
	},
	"dependencies": {
		"zod": "4.3.5"
	}
}
